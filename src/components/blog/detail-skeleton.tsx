import * as React from "react";

interface IDetailSkeletonProps {}

const DetailSkeleton: React.FunctionComponent<IDetailSkeletonProps> = (
  props
) => {
  return <></>;
};

export default DetailSkeleton;
